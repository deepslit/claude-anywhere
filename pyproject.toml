[project]
name = "mobile-cc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "pathspec>=1.1.1",
    "pydantic-settings>=2.14.1",
    "uvicorn[standard]>=0.46.0",
]

[project.scripts]
mobile-cc = "mobile_cc:main"

[build-system]
requires = ["uv_build>=0.9.21,<0.10.0"]
build-backend = "uv_build"
